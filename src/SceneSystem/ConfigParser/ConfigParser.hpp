/**
 * @file ConfigParser.hpp
 * @brief Configuration parser for the ray tracer application
 * 
 * @details This file defines the ConfigParser class, which is responsible for parsing configuration files for the ray tracer application.
 * The ConfigParser reads a specific file format and turns it into a map of key-value pairs that can be used by the SceneLoader to configure various aspects of the ray tracer,
 * such as scene settings, rendering options, and plugin configurations.
 */